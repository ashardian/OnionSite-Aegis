# Additional Anti-Tracking Nginx Configuration
# Include this in your nginx config if lua module is available
# This provides response padding and timing randomization

# Uncomment if nginx-lua is available:
# init_by_lua_block {
#     local padding = require "response_padding"
#     _G.padding_module = padding
# }

# In location blocks, add:
# body_filter_by_lua_block {
#     _G.padding_module.add_padding()
# }

# For timing randomization:
# access_by_lua_block {
#     _G.padding_module.add_delay()
# }

# Alternative: Use nginx post_action for basic padding (no lua required)
# This is a simpler approach that works without lua module

